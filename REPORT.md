# Отчёт по лабораторной работе №2
## Управление конкурентными транзакциями в маркетплейсе

**Студент:** _[Дарья Ванина]_  
**Дата:** _[28.03.2026]_

---

## Раздел 1: Описание проблемы

### Что такое Race Condition?

_TODO: Объясните, что такое race condition в контексте баз данных._
Race condition (состояние гонки) — это ситуация, когда результат работы 
программы зависит от порядка выполнения параллельных операций. 
В контексте баз данных: две транзакции читают одни и те же данные, 
принимают решение на основе прочитанного, и обе пишут результат — 
не зная об изменениях друг друга.
**Пример из жизни:**
- Пользователь нажимает кнопку "Оплатить" дважды — два запроса уходят почти одновременно, оба видят статус 'created' и оба пытаются оплатить заказ.

### Почему READ COMMITTED не защищает от двойной оплаты?

_TODO: Объясните механизм работы READ COMMITTED и почему он не предотвращает race condition._

На уровне изоляции READ COMMITTED:
1. Каждая транзакция видит только закоммиченные данные на момент каждого отдельного SELECT
2. Когда две транзакции одновременно читают статус 'created' — обе принимают решение что оплата возможна
3. В результате обе записывают статус 'paid', не зная об изменениях друг друга до коммита

**Демонстрация проблемы:**

```
Время | Сессия 1                    | Сессия 2
------|----------------------------|---------------------------
t1    | BEGIN                      |
t2    | SELECT status (created)    |
t3    |                            | BEGIN
t4    |                            | SELECT status (created)
t5    | pg_sleep(3)                |
t6    |                            | pg_sleep(3)
t7    | UPDATE status = paid       |
t8    |                            | UPDATE status = paid (ждет)
t9    | COMMIT                     |
t10   |                            | UPDATE выполняется...
t11   |                            | COMMIT
```

_TODO: Объясните, что происходит на каждом этапе._
t1-t2: Сессия 1 начинает транзакцию и читает статус 'created'
t3-t4: Сессия 2 тоже начинает и тоже читает 'created' — обе видят одно и то же
t7:    Сессия 1 обновляет статус на 'paid' и делает COMMIT
t10:   Сессия 2 тоже обновляет на 'paid' — она не знала об изменениях Сессии 1
t11:   Итог: заказ оплачен дважды, в истории две записи 'paid'

### Примеры из реальной жизни

_TODO: Приведите реальные примеры, когда может возникнуть такая проблема._

1. **Двойной клик на кнопку "Оплатить"**
   - Пользователь нажимает кнопку дважды
   - Два HTTP-запроса приходят почти одновременно
   - Оба запроса начинают обработку параллельно

2. **Микросервисная архитектура**
   - Payment-сервис получает событие об оплате через очередь сообщений
   - Из-за сбоя событие доставляется дважды
   - Два экземпляра сервиса обрабатывают одно и то же событие одновременно

3. **Сбой сети**
   - Клиент отправил запрос на оплату, но не получил ответ из-за таймаута
   - Клиент повторяет запрос
   - Первый запрос на самом деле дошёл и обрабатывается параллельно со вторым


---

## Раздел 2: Уровни изоляции в PostgreSQL

### READ UNCOMMITTED

**Описание:**
_TODO: Опишите этот уровень изоляции._
Самый слабый уровень изоляции. Транзакция может читать данные,
которые ещё не были закоммичены другими транзакциями (dirty reads).
В PostgreSQL фактически работает как READ COMMITTED из-за архитектуры MVCC — грязное чтение в PostgreSQL невозможно даже на этом уровне.

**Предотвращает:**
- _TODO: Список аномалий, которые предотвращаются._
- Ничего из перечисленных ниже аномалий


**Не предотвращает:**
- Dirty reads
- Non-repeatable reads
- Phantom reads

**Когда использовать:**
_TODO: В каких случаях этот уровень подходит._
Практически никогда в продакшене. Теоретически подходит для приближённой
аналитики где абсолютная точность не важна и важна скорость. В PostgreSQL
нет смысла использовать — он ведёт себя как READ COMMITTED.

**Особенность в PostgreSQL:**
В PostgreSQL READ UNCOMMITTED работает как READ COMMITTED из-за архитектуры MVCC.

---

### READ COMMITTED (по умолчанию)

**Описание:**
_TODO: Опишите этот уровень изоляции._
Транзакция видит только закоммиченные данные. Каждый SELECT в транзакции
видит актуальный срез данных на момент выполнения именно этого запроса,
а не на момент начала транзакции.

Каждый SELECT в транзакции видит данные, закоммиченные непосредственно
перед выполнением этого конкретного запроса.

**Предотвращает:**
- ✅ Dirty reads (чтение незакоммиченных данных)

**Не предотвращает:**
- ❌ Non-repeatable reads (повторное чтение дает другой результат)
- ❌ Phantom reads (новые строки появляются в результате запроса)

**Пример non-repeatable read:**
```sql
-- Сессия 1
BEGIN;
SELECT balance FROM accounts WHERE id = 1; -- Результат: 1000
-- Сессия 2 изменяет balance и делает COMMIT
SELECT balance FROM accounts WHERE id = 1; -- Результат: 500 (!)
COMMIT;
```

**Когда использовать:**
_TODO: В каких случаях этот уровень подходит._
Для большинства обычных операций: просмотр каталога товаров,
чтение истории заказов, получение профиля пользователя.
Это дефолтный уровень PostgreSQL — быстрый и достаточно безопасный
для некритичных операций где точная согласованность внутри транзакции не важна.

---

### REPEATABLE READ

**Описание:**
_TODO: Опишите этот уровень изоляции._
Транзакция видит snapshot данных на момент начала транзакции.
Все SELECT внутри одной транзакции возвращают одинаковые данные
независимо от того, какие коммиты сделали другие транзакции в это время.

Транзакция видит snapshot данных на момент начала первого запроса
в транзакции и не видит изменений, сделанных другими транзакциями после этого.

**Предотвращает:**
- ✅ Dirty reads
- ✅ Non-repeatable reads
- ✅ Phantom reads (в PostgreSQL благодаря MVCC)

**Не предотвращает:**
- ❌ Write skew (специфичная аномалия)
- ❌ Некоторые сериализационные аномалии

**Особенность в PostgreSQL:**
В отличие от стандарта SQL, PostgreSQL на REPEATABLE READ также предотвращает phantom reads.

**Когда использовать:**
_TODO: В каких случаях этот уровень подходит._
Для операций где важна согласованность данных внутри транзакции:
оплата заказа, изменение баланса, резервирование товара на складе.
Используется вместе с FOR UPDATE для надёжной защиты от race condition.

---

### SERIALIZABLE

**Описание:**
_TODO: Опишите этот уровень изоляции._
Самый строгий уровень, гарантирующий что транзакции выполняются так,
как будто они идут строго последовательно, одна за другой.
PostgreSQL отслеживает зависимости между транзакциями и при обнаружении
конфликта откатывает одну из них с ошибкой serialization failure.

**Предотвращает:**
- ✅ Все аномалии чтения
- ✅ Сериализационные аномалии
- ✅ Write skew

**Недостатки:**
- ❌ Может откатывать транзакции при конфликтах (serialization failure)
- ❌ Снижение производительности
- ❌ Требует retry logic в приложении

**Пример serialization failure:**
```sql
-- Сессия 1
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 5000
UPDATE accounts SET balance = balance + 100 WHERE id = 1;

-- Сессия 2 (параллельно)
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 5000
UPDATE accounts SET balance = balance + 200 WHERE id = 2;

-- При COMMIT одна из транзакций получит ошибку:
-- ERROR: could not serialize access due to read/write dependencies
```

**Когда использовать:**
_TODO: В каких случаях этот уровень подходит._
Для финансовых операций с максимальными требованиями к корректности,
когда цена ошибки очень высока и допустимы повторные попытки.
Например, банковские переводы между счетами или биржевые операции.

---

### Сравнительная таблица

| Уровень изоляции  | Dirty Read | Non-Repeatable Read | Phantom Read | Performance | Use Case |
|-------------------|------------|---------------------|--------------|-------------|----------|
| READ UNCOMMITTED  | ❌          | ❌                   | ❌            | Высокая     | Аналитика (неточная) |
| READ COMMITTED    | ✅          | ❌                   | ❌            | Высокая     | Обычные операции |
| REPEATABLE READ   | ✅          | ✅                   | ✅*           | Средняя     | Критичные операции |
| SERIALIZABLE      | ✅          | ✅                   | ✅            | Низкая      | Финансовые транзакции |

_*В PostgreSQL REPEATABLE READ также предотвращает phantom reads благодаря MVCC._

---

## Раздел 3: Решение проблемы

### Почему REPEATABLE READ решает проблему?

_TODO: Объясните, как REPEATABLE READ помогает решить проблему конкурентных оплат._

REPEATABLE READ использует snapshot isolation:
1. Транзакция видит стабильный snapshot данных на весь период своей работы
2. Это означает, что повторный SELECT внутри транзакции вернёт те же данные,
   даже если другая транзакция уже сделала изменения и COMMIT
3. Однако, без блокировок обе транзакции всё равно могут записать 'paid' —
   PostgreSQL не знает что они конфликтуют, пока не происходит попытка записи

### Зачем нужен FOR UPDATE?

_TODO: Объясните роль FOR UPDATE в решении проблемы._

`FOR UPDATE` создает эксклюзивную блокировку на уровне строки (row-level lock):

**Без FOR UPDATE:**
```sql
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...';
-- Другая транзакция может прочитать ту же строку
UPDATE orders SET status = 'paid' WHERE id = '...';
COMMIT;
```

**С FOR UPDATE:**
```sql
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...' FOR UPDATE;
-- Другая транзакция ЖДЕТ освобождения блокировки
UPDATE orders SET status = 'paid' WHERE id = '...';
COMMIT;
```

**Типы блокировок:**
- `FOR UPDATE` — эксклюзивная блокировка (никто не может изменить или заблокировать)
- `FOR SHARE` — разделяемая блокировка (другие могут читать с FOR SHARE, но не могут изменять)
- `FOR NO KEY UPDATE` — как FOR UPDATE, но разрешает concurrent FOR KEY SHARE
- `FOR KEY SHARE` — слабая блокировка (предотвращает DELETE и UPDATE ключевых полей)

### Что произойдет без FOR UPDATE на REPEATABLE READ?

_TODO: Опишите сценарий, когда REPEATABLE READ без FOR UPDATE не работает._

Даже на REPEATABLE READ возможна аномалия:

```sql
-- Сессия 1
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...'; -- created
-- Задержка...
UPDATE orders SET status = 'paid' WHERE id = '...' AND status = 'created';
COMMIT;

-- Сессия 2 (параллельно)
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...'; -- created (snapshot!)
-- Задержка...
UPDATE orders SET status = 'paid' WHERE id = '...' AND status = 'created';
-- ЧТО ПРОИЗОЙДЕТ?
```

_TODO: Объясните, что произойдет в этом случае._
Без FOR UPDATE обе транзакции прочитают статус 'created' из своего snapshot.
Обе пройдут проверку и выполнят UPDATE. В PostgreSQL UPDATE на REPEATABLE READ
видит актуальные данные (не snapshot), поэтому вторая транзакция обновит
уже изменённую строку. Но INSERT в order_status_history всё равно запишет
вторую запись 'paid' — race condition сохраняется.
С FOR UPDATE вторая транзакция будет ждать пока первая не сделает COMMIT,
затем увидит что статус уже 'paid' и выбросит исключение.


### Разница между FOR UPDATE и FOR SHARE

| Характеристика | FOR UPDATE | FOR SHARE |
|----------------|------------|-----------|
| Тип блокировки | Эксклюзивная | Разделяемая |
| Блокирует чтение с FOR UPDATE | ✅ | ✅ |
| Блокирует чтение с FOR SHARE | ✅ | ❌ |
| Блокирует UPDATE | ✅ | ✅ |
| Блокирует DELETE | ✅ | ✅ |
| Use case | Перед изменением | Защита от изменений |

**Пример использования FOR SHARE:**
```sql
-- Проверка наличия товара перед созданием заказа
SELECT quantity FROM products WHERE id = '...' FOR SHARE;
-- Товар не может быть удален или изменен до COMMIT
```

---

## Раздел 4: Рекомендации для продакшена

### Какой ISOLATION LEVEL использовать для продакшена маркетплейса?

_TODO: Дайте обоснованную рекомендацию по выбору уровня изоляции для продакшена._

**Рекомендация:** Для продакшена маркетплейса рекомендуется использовать **READ COMMITTED как default уровень** с явным использованием **REPEATABLE READ + FOR UPDATE для критичных операций**.

#### Обоснование:

**1. Производительность**
- READ COMMITTED имеет минимальный overhead
- REPEATABLE READ используется только там, где необходимо
- Избегаем глобального использования SERIALIZABLE
- Блокировки на уровне строк (row-level locks) не мешают параллельной
  обработке других заказов — каждый заказ блокируется независимо
- Connection pooling работает эффективно, транзакции короткие

_TODO: Расширьте обоснование._

**2. Безопасность данных**
- Критичные операции (оплата, изменение баланса) защищены через REPEATABLE READ + FOR UPDATE
- Некритичные операции (просмотр каталога) работают на READ COMMITTED
- Четкое разделение зон ответственности
- Явное использование FOR UPDATE делает код самодокументируемым —
  разработчик сразу видит что операция критична и требует блокировки

_TODO: Расширьте обоснование._

**3. Риски deadlock**
- FOR UPDATE может привести к deadlock при неправильном порядке блокировок
- Решение: всегда блокировать ресурсы в одном порядке
- Пример: сначала order, потом order_items, потом payment
- PostgreSQL автоматически обнаруживает deadlock и откатывает одну
  из транзакций — важно обрабатывать это исключение в приложении


_TODO: Расширьте обоснование._

**4. Простота разработки**
- READ COMMITTED легко понять и отлаживать
- REPEATABLE READ + FOR UPDATE требует explicit intent
- Код становится самодокументируемым
- Новые разработчики в команде легко понимают где нужна особая осторожность

_TODO: Расширьте обоснование._

**5. Масштабируемость**
- READ COMMITTED хорошо масштабируется
- Локальное использование блокировок не создает bottleneck
- Connection pooling работает эффективно
- При росте нагрузки можно добавить read replicas для некритичных операций

_TODO: Расширьте обоснование._

### Альтернативные подходы

#### Подход 1: Использовать SERIALIZABLE везде

**Плюсы:**
- Максимальная корректность
- Не нужно думать о блокировках
- PostgreSQL автоматически обнаруживает конфликты

**Минусы:**
- Значительное снижение производительности (20-50%)
- Требует retry logic во всех операциях
- Высокий процент rollback при нагрузке
- Сложная отладка serialization failures

**Вывод:** Не рекомендуется для high-load систем.

#### Подход 2: Optimistic Locking (версионирование)

**Реализация:**
```sql
ALTER TABLE orders ADD COLUMN version INTEGER DEFAULT 1;

UPDATE orders 
SET status = 'paid', version = version + 1
WHERE id = '...' AND status = 'created' AND version = 1;

-- Проверить ROW_COUNT, если 0 — конфликт
```

**Плюсы:**
- Нет блокировок на чтение
- Хорошая производительность
- Масштабируется горизонтально

**Минусы:**
- Требует изменения схемы БД
- Требует retry logic в приложении
- Может быть много конфликтов при высокой нагрузке

**Вывод:** Хороший подход для распределенных систем.

#### Подход 3: Advisory Locks

**Реализация:**
```sql
BEGIN;
SELECT pg_advisory_xact_lock(hashtext('order_' || order_id));
-- Критическая секция
COMMIT; -- Блокировка автоматически снимается
```

**Плюсы:**
- Гибкий контроль блокировок
- Можно блокировать логические ресурсы
- Работает на любом уровне изоляции

**Минусы:**
- Легко забыть снять блокировку
- Сложнее отлаживать
- Требует дисциплины от разработчиков

**Вывод:** Полезно для специфичных случаев, но не как основной подход.

### Итоговая рекомендация

Для продакшена маркетплейса используйте **гибридный подход**:

1. **Default isolation level:** `READ COMMITTED`
   - Для обычных операций (чтение каталога, просмотр истории)
   - 95% операций

2. **REPEATABLE READ + FOR UPDATE:** для критичных операций
   - Оплата заказа
   - Изменение баланса
   - Резервирование товара
   - 5% операций

3. **Дополнительно:** Optimistic locking для операций с высокой конкурентностью
   - Обновление счетчиков
   - Изменение рейтингов

**Пример кода:**
```python
async def pay_order(order_id: UUID):
    async with db.transaction(isolation='repeatable_read'):
        # Заблокировать заказ
        order = await db.fetch_one(
            "SELECT * FROM orders WHERE id = $1 FOR UPDATE",
            order_id
        )
        
        if order['status'] != 'created':
            raise OrderAlreadyPaidError()
        
        # Обновить статус
        await db.execute(
            "UPDATE orders SET status = 'paid' WHERE id = $1",
            order_id
        )
        
        # Записать в историю
        await db.execute(
            "INSERT INTO order_status_history (order_id, status) VALUES ($1, 'paid')",
            order_id
        )
```

---

## Заключение

_TODO: Напишите краткое заключение о проделанной работе._

В ходе лабораторной работы было изучено:
1. Механизм возникновения race condition при конкурентных транзакциях в PostgreSQL
2. Уровни изоляции транзакций и их практические отличия
3. Использование REPEATABLE READ + FOR UPDATE для предотвращения двойной оплаты

Основные выводы:
- READ COMMITTED недостаточен для критичных финансовых операций — две параллельные транзакции могут оба прочитать статус 'created' и оба записать 'paid'
- REPEATABLE READ + FOR UPDATE надёжно предотвращает race condition — вторая транзакция ждёт снятия блокировки и после видит уже изменённый статус
- Гибридный подход (READ COMMITTED по умолчанию + REPEATABLE READ для критичных операций) оптимален для продакшена по соотношению безопасности и производительности

---

## Приложение: Результаты тестирования

### Тест 1: Демонстрация проблемы (READ COMMITTED)

_TODO: Вставьте скриншоты или вывод команд, демонстрирующие двойную оплату._
⚠️ RACE CONDITION DETECTED!
Order 0982164a-92ff-4b2c-894d-0a0fb4b8ed9f was paid TWICE:
  - 2026-03-27 19:31:21.664502+00:00: status = paid
  - 2026-03-27 19:31:21.700855+00:00: status = paid
```

### Тест 2: Решение проблемы (REPEATABLE READ + FOR UPDATE)

_TODO: Вставьте скриншоты или вывод команд, демонстрирующие корректную работу._

✅ RACE CONDITION PREVENTED!
Order 10c5c410-054a-40ed-863c-ee8c50d2887d was paid only ONCE:
  - 2026-03-27 19:31:45.135451+00:00: status = paid
Second attempt was rejected: Order 10c5c410-054a-40ed-863c-ee8c50d2887d is already paid
